<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TCC.Views.ChoosePagePassenger"
             Shell.NavBarIsVisible="False">

    <Grid>
        <!-- Imagem de fundo -->
        <Image Source="fundo_tcc.jpg"
           Aspect="AspectFill"
           HorizontalOptions="FillAndExpand"
           VerticalOptions="FillAndExpand" />

        <!-- Conteúdo principal -->
        <VerticalStackLayout Spacing="40"
                             Padding="0"
                             VerticalOptions="Center"
                             HorizontalOptions="Center">

            <!-- Texto de Boas-vindas -->
            <Label Text="Menu do Passageiro"
                   FontSize="36"
                   FontAttributes="Bold"
                   TextColor="White"
                   HorizontalOptions="Center"
                   VerticalOptions="Center">
                <Label.Shadow>
                    <Shadow Brush="Black" 
                            Offset="2,2" 
                            Radius="5" 
                            Opacity="0.3"/>
                </Label.Shadow>
            </Label>

            <!-- Botões -->
            <VerticalStackLayout Spacing="20" 
                                 WidthRequest="250">

                <!-- Botão Vou Hoje / Não Vou Hoje -->
                <Button x:Name="EVH"
                        Text="✓ Vou hoje"
                        TextColor="White"
                        FontAttributes="Bold"
                        BackgroundColor="#28a745"
                        CornerRadius="25"
                        HeightRequest="55"
                        SemanticProperties.Hint="Confirmar presença hoje"
                        Clicked="EVH_Clicked">
                    <Button.Shadow>
                        <Shadow Brush="Black" 
                                Offset="0,4" 
                                Radius="8" 
                                Opacity="0.2"/>
                    </Button.Shadow>
                </Button>

                <!-- Botão Perfil -->
                <Button x:Name="perfilButton"
                        Text="👤 Perfil"
                        TextColor="#2C3E50"
                        FontAttributes="Bold"
                        BackgroundColor="White"
                        CornerRadius="25"
                        HeightRequest="55"
                        SemanticProperties.Hint="Ver meu perfil"
                        Clicked="PerfilButton_Clicked">
                    <Button.Shadow>
                        <Shadow Brush="Black" 
                                Offset="0,4" 
                                Radius="8" 
                                Opacity="0.2"/>
                    </Button.Shadow>
                </Button>

                <!-- Botão Grupos -->
                <Button x:Name="gruposButton"
                        Text="👥 Grupos"
                        TextColor="#2C3E50"
                        FontAttributes="Bold"
                        BackgroundColor="White"
                        CornerRadius="25"
                        HeightRequest="55"
                        SemanticProperties.Hint="Ver grupos"
                        Clicked="GruposButton_Clicked">
                    <Button.Shadow>
                        <Shadow Brush="Black" 
                                Offset="0,4" 
                                Radius="8" 
                                Opacity="0.2"/>
                    </Button.Shadow>
                </Button>

                <!-- Botão Desconectar -->
                <Button x:Name="desconectarButton"
                        Text="❌ Desconectar"
                        TextColor="White"
                        FontAttributes="Bold"
                        BackgroundColor="Transparent"
                        BorderColor="White"
                        BorderWidth="2"
                        CornerRadius="25"
                        HeightRequest="55"
                        SemanticProperties.Hint="Sair da conta"
                        Clicked="DesconectarButton_Clicked">
                    <Button.Shadow>
                        <Shadow Brush="Black" 
                                Offset="0,4" 
                                Radius="8" 
                                Opacity="0.2"/>
                    </Button.Shadow>
                </Button>

            </VerticalStackLayout>

        </VerticalStackLayout>

        <!-- Botão de Ajuda no canto inferior direito -->
        <Button x:Name="ajudaButton"
                Text="?"
                TextColor="White"
                FontSize="24"
                FontAttributes="Bold"
                BackgroundColor="#007bff"
                CornerRadius="30"
                WidthRequest="60"
                HeightRequest="60"
                HorizontalOptions="End"
                VerticalOptions="End"
                Margin="0,0,20,20"
                SemanticProperties.Hint="Ajuda"
                Clicked="AjudaButton_Clicked">
            <Button.Shadow>
                <Shadow Brush="Black" 
                        Offset="0,4" 
                        Radius="8" 
                        Opacity="0.3"/>
            </Button.Shadow>
        </Button>

        <!-- Popup de Ajuda -->
        <Frame x:Name="ajudaPopup"
               IsVisible="False"
               BackgroundColor="White"
               CornerRadius="15"
               Padding="20"
               HasShadow="True"
               HorizontalOptions="Center"
               VerticalOptions="Center"
               WidthRequest="320"
               MaximumHeightRequest="500">
            <ScrollView>
                <VerticalStackLayout Spacing="15">

                    <!-- Título -->
                    <Label Text="ℹ️ Ajuda"
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="#2C3E50"
                           HorizontalOptions="Center"
                           Margin="0,0,0,10"/>

                    <!-- Explicação Vou Hoje / Não Vou Hoje -->
                    <VerticalStackLayout Spacing="5">
                        <Label Text="✓ Vou hoje / ❌ Não vou hoje"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="#28a745"/>
                        <Label TextColor="#555">
                            <Label.FormattedText>
                                <FormattedString>
                                    <Span Text="• Quando exibir "/>
                                    <Span Text="'Vou hoje'" FontAttributes="Bold"/>
                                    <Span Text=": você será pego pelo transporte."/>
                                    <Span Text="&#x0a;• Quando exibir "/>
                                    <Span Text="'Não vou hoje'" FontAttributes="Bold"/>
                                    <Span Text=": você NÃO será pego pelo transporte."/>
                                    <Span Text="&#x0a;• Clique no botão para alternar entre os estados."/>
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>
                    </VerticalStackLayout>

                    <BoxView HeightRequest="1" Color="#E0E0E0"/>

                    <!-- Explicação Perfil -->
                    <VerticalStackLayout Spacing="5">
                        <Label Text="👤 Perfil"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="#2C3E50"/>
                        <Label Text="Visualize e edite suas informações pessoais, endereço e dados de contato."
                               TextColor="#555"
                               LineBreakMode="WordWrap"/>
                    </VerticalStackLayout>

                    <BoxView HeightRequest="1" Color="#E0E0E0"/>

                    <!-- Explicação Grupos -->
                    <VerticalStackLayout Spacing="5">
                        <Label Text="👥 Grupos"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="#2C3E50"/>
                        <Label Text="Acesse seus grupos de transporte, veja informações sobre rotas e outros passageiros."
                               TextColor="#555"
                               LineBreakMode="WordWrap"/>
                    </VerticalStackLayout>

                    <BoxView HeightRequest="1" Color="#E0E0E0"/>

                    <!-- Explicação Desconectar -->
                    <VerticalStackLayout Spacing="5">
                        <Label Text="❌ Desconectar"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="#dc3545"/>
                        <Label Text="Saia da sua conta com segurança."
                               TextColor="#555"
                               LineBreakMode="WordWrap"/>
                    </VerticalStackLayout>

                    <!-- Botão Fechar -->
                    <Button Text="Fechar"
                            TextColor="White"
                            FontAttributes="Bold"
                            BackgroundColor="#007bff"
                            CornerRadius="20"
                            HeightRequest="45"
                            Margin="0,10,0,0"
                            Clicked="FecharAjudaButton_Clicked"/>

                </VerticalStackLayout>
            </ScrollView>
        </Frame>

        <!-- Overlay escuro quando popup está aberto -->
        <BoxView x:Name="ajudaOverlay"
                 IsVisible="False"
                 Color="Black"
                 Opacity="0.5"
                 HorizontalOptions="FillAndExpand"
                 VerticalOptions="FillAndExpand">
            <BoxView.GestureRecognizers>
                <TapGestureRecognizer Tapped="FecharAjudaButton_Clicked"/>
            </BoxView.GestureRecognizers>
        </BoxView>

    </Grid>
</ContentPage>