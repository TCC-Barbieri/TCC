<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TCC.Views.ConfigurarViagemPage"
             BackgroundColor="#F5F5F5"
             Shell.NavBarIsVisible="False">

    <ScrollView>
        <VerticalStackLayout Spacing="0">

            <!-- Header com gradiente vermelho -->
            <Border BackgroundColor="#e86464" 
                    Padding="20,30" 
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle,0,0,15,15">

                <Grid>
                    <Path Fill="#e04141"
                          Opacity="0.3"
                          Data="M0,20 C100,0 200,40 400,20 L400,0 L0,0 Z"
                          HorizontalOptions="FillAndExpand"
                          VerticalOptions="Start"
                          Aspect="Fill" />

                    <VerticalStackLayout HorizontalOptions="Center" Spacing="5">
                        <Label Text="🚗 Configurar Viagem" 
                               FontSize="24" 
                               HorizontalOptions="Center" 
                               FontAttributes="Bold"
                               TextColor="White">
                            <Label.Shadow>
                                <Shadow Brush="Black" Offset="2,2" Radius="5" Opacity="0.3"/>
                            </Label.Shadow>
                        </Label>
                        <Label Text="Defina os detalhes da sua viagem" 
                               FontSize="14" 
                               HorizontalOptions="Center" 
                               TextColor="White" />
                    </VerticalStackLayout>
                </Grid>
            </Border>

            <!-- Configurações da Viagem -->
            <Frame BackgroundColor="White"
                   Margin="20,10,20,10"
                   CornerRadius="15"
                   HasShadow="True"
                   Padding="20">
                <VerticalStackLayout Spacing="20">
                    <Label Text="Detalhes da Viagem" 
                           FontSize="18" 
                           FontAttributes="Bold" 
                           TextColor="#333" 
                           HorizontalOptions="Center" />

                    <!-- Turmas/Escolas -->
                    <VerticalStackLayout Spacing="8">
                        <HorizontalStackLayout Spacing="8">
                            <Frame BackgroundColor="#FF5722" 
                                   CornerRadius="12" 
                                   Padding="8" 
                                   HasShadow="False">
                                <Label Text="🎒" FontSize="12" TextColor="White" />
                            </Frame>
                            <Label Text="Escola/Turma" 
                                   FontSize="14" 
                                   FontAttributes="Bold" 
                                   TextColor="#333" 
                                   VerticalOptions="Center" />
                        </HorizontalStackLayout>
                        <Border Stroke="#DDDDDD" 
                                StrokeThickness="1" 
                                StrokeShape="RoundRectangle 8,8,8,8" 
                                BackgroundColor="White">
                            <Picker x:Name="TurmaPicker" 
                                    FontSize="14"
                                    HeightRequest="45"
                                    SelectedIndexChanged="OnTurmaPickerChanged">
                                <Picker.Items>
                                    <x:String>ETEC</x:String>
                                    <x:String>UNESP</x:String>
                                    <x:String>Unisagrado</x:String>
                                    <x:String>UNOESTE</x:String>
                                </Picker.Items>
                            </Picker>
                        </Border>

                        <!-- Contador de passageiros -->
                        <HorizontalStackLayout Spacing="5" Margin="0,5,0,0">
                            <Label Text="👥" FontSize="12" />
                            <Label x:Name="PassengersCountLabel" 
                                   Text="Selecione uma turma para ver os passageiros" 
                                   FontSize="12" 
                                   TextColor="#666" />
                        </HorizontalStackLayout>
                    </VerticalStackLayout>

                    <!-- Local de Destino -->
                    <VerticalStackLayout Spacing="8">
                        <HorizontalStackLayout Spacing="8">
                            <Frame BackgroundColor="#FF5722" 
                                   CornerRadius="12" 
                                   Padding="8" 
                                   HasShadow="False">
                                <Label Text="🎯" FontSize="12" TextColor="White" />
                            </Frame>
                            <Label Text="Local de Destino" 
                                   FontSize="14" 
                                   FontAttributes="Bold" 
                                   TextColor="#333" 
                                   VerticalOptions="Center" />
                        </HorizontalStackLayout>
                        <Border Stroke="#DDDDDD" 
                                StrokeThickness="1" 
                                StrokeShape="RoundRectangle 8,8,8,8" 
                                BackgroundColor="White">
                            <Picker x:Name="LocalDestinoPicker" 
                                    FontSize="14"
                                    HeightRequest="45">
                                <Picker.Items>
                                    <x:String>ETEC Joaquim Ferreira do Amaral</x:String>
                                    <x:String>UNESP</x:String>
                                    <x:String>Unisagrado</x:String>
                                    <x:String>UNOESTE</x:String>
                                    <x:String>GARAGEM</x:String>
                                </Picker.Items>
                            </Picker>
                        </Border>
                    </VerticalStackLayout>

                </VerticalStackLayout>
            </Frame>

            <!-- Botões de Ação -->
            <VerticalStackLayout Spacing="15" Margin="20,10,20,30">

                <!-- Botão Iniciar Viagem -->
                <Button x:Name="IniciarViagemButton"
                        Text="🚀 INICIAR VIAGEM"
                        BackgroundColor="#4CAF50"
                        TextColor="White"
                        FontAttributes="Bold"
                        CornerRadius="25"
                        HeightRequest="55"
                        FontSize="16"
                        Clicked="OnIniciarViagemClicked">
                    <Button.Shadow>
                        <Shadow Brush="Black" Offset="0,4" Radius="8" Opacity="0.2"/>
                    </Button.Shadow>
                </Button>

                <!-- Botão Voltar -->
                <Button x:Name="VoltarButton"                      
                        Text="← Voltar ao Menu"
                        BackgroundColor="Transparent"
                        BorderColor="#e86464"
                        BorderWidth="2"
                        TextColor="#e86464"
                        FontAttributes="Bold"
                        CornerRadius="25"
                        HeightRequest="50"
                        FontSize="14"
                        Clicked="OnVoltarClicked">
                    <Button.Shadow>
                        <Shadow Brush="Black" Offset="0,4" Radius="8" Opacity="0.2"/>
                    </Button.Shadow>
                </Button>

            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>